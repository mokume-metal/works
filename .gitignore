.build/
.swiftpm/
.mokume/
shots/
out/
out-*/
upstream/

# **スケッチが自分で書き出したもの。** `Topics/File IO/SaveOneImage` は押されたら
# `save("line.png")` を呼ぶ作りで、`Atlas --motion` が道すじの途中でマウスを押すため
# 撮影のたびに作業ディレクトリへ残る。mokume v0.6.0 で押下を受け取れるようになって
# 初めて出るようになったもので、**証跡を撮ると副作用が走る**ことの記録でもある。
line.png
